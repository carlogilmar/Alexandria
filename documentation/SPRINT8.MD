# Sprint 8

Next features to implement:
- The current version is very good. Now we need to start to think about what happen when I'll have a lot of workflows and notes, the sidebar is useful for the latest ones, let's keep just the lasts ones and pinned ones in the sidebar, and in the welcome page we already have the counters that open the list of everything.
- Let's implement a new entity: articles. Now we have tasks, lists, notes and workflows, every one have an ID to be linked somewhere like our summary page. Let's introduce articles as notes where we can refer any of the other elements, but now we need to see that elements inside as part of the body, not just a link, it's an embedded element. This will help me to create a better knowledge system. Also we should be able to add images as well.
- The index page is good, but we need to work on the UX to let the user this is the most important part because is the summary of everything, also the button to go here is very shy, we need to put a button with the proper label to make sure it's clear for the user this is for go to the index. 
