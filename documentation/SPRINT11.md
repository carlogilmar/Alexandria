# Sprint 11


## Improvements

Now we have different features, let's go for some fixes:

- Let's redefine the index-summary, let's rename this into "Summary", currently it's a text note where I can refer all the content manually, instead of this, let's use this place to put the list of everything, at the top of the list I want to see the most recent files, probably add some tabs to show workflows, articles, notes, and lists. Also in the list I want to be able to delete, show, or archive. I need to archive content because there are some content on-the-fly, that means this specific content is useful for a short time and the it evolve into a note or workflow, so this content becomes unuseful, but I don't want to remove it, so I need to be able to archive but still be able to see the content. 
- Garden: Let's rename this into "Visualization" and let's put the radial view as the default view. 
- Currently we have 4 important buttons: home, index, garden and map. As this are important parts, I struggle with the UI to move quickly here, maybe some shortcuts could improve this part, also we need to improve the UX for this buttons, probably at the bottom of the sidebar I'd love to put the buttons for summary and visualization, the most important ones are home and map. Let's rename map to "Library of Alexandria" as a metaphor this is a knowledge base. 
- In the sidebar instead of list all entities, let's just show pinned ones, the summary will help us to pin important content. Instead of have one button there for add a new note or new article or new workflow, let's add only one button and let the use choose if he wants a note, an article, or a workflow.
- Currently with our design it's easy to miss the current todo list of the day, for that, we need to show a direct access to the current list, we need to validate if exist, if not we need to show the user the button to create the list of day. 

## Map - Library of Alexandria

This is the place I want to use as a big picture of the content I have, it's a place to struture my notes, concepts, and formalize connections, that's why it's important to improve constantly.

- Currently the connection lines between nodes are fine, but I'd love to see an arrow just to see the flow of the connections.
- We have notes, but I'd love to put text labels to complement the diagram, this just as simple notes without connections.
- The current nodes are the entities we have, but I'd love to add new nodes that contains just text.- In the menu to add to map I'd love to drag and drop the nodes. After add a new node from here the menu should collapse.
- Text nodes are good but I'd love to resize the whole node to adapt better the text inside.
- Still looks pixelated.


