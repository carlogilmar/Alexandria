# Sprint 7 — Suggestions: from TODO list to knowledge system

Next features to implement:

- I like every item on my list have a detailed view with notes, but the section to see the notes, not the editor, when the text is too big the UI is broken.
- Let's add a new feature called "Notes", I'd love to edit in markdown but see render as proper formatted text. So every day in calendar I'll see the option to create a new list but also a new note attached to that day.
- Now the logo image is a button for home, but let's update this part, every time I click on the logo I want to see the home page with the current day so I'll see the todo list of today and also the notes
- Index summary: now we'll have notes, tasks in lists, and workflows. I'd love to have an index file, in this place I want to use markdown text to list all the important content (title with link to any task, note or workflow) so this will help me to summarize all the important information in just one place. I need to see this in formatted text, but editable with markdown. I like the way of see the text formatted and click for edit and see the editor.
